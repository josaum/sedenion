# 2026-06-01

- Rewrote the root `README.md` comprehensively as the repo front door: thesis, Rust 1.96.0 pin, crate deliverable/API, repr-bakeoff positive narrow result, nav-bakeoff negative result, reproducibility commands, evidence map, layout, and explicit non-claims.
- Added `nav-repr-bakeoff` as the constructive follow-up to the negative navigation result: keep the classical navigation state, build 3x16 IMU-window summaries, and compare train mean, dense 48->16, sedenion left/right 16-D, and sedenion+auto-ZDA on current accel bias plus future drift prediction. Also added filter-in-loop bias pseudo-measurement evaluation. First checked 180 s non-Duffing run: dense is the current held-out/proxy winner and reduces no-fix terminal RMSE from 531.46 m to 428.43 m; sedenion+auto-ZDA gets proxy MSE 1.2394 and terminal 517.47 m, so it is not a state-manifold rescue or current representation win.
- Revised `nav-repr-bakeoff` toward a JEPA-style trajectory objective: each example now has a context IMU window and held-out future window; added dense and sedenion JEPA pretraining with stopped-gradient future latent, frozen-probe heads, and JEPA+supervised fine-tune rows. Current 64/16 seed, 180 s non-Duffing result: dense JEPA+fine-tune improves proxy test MSE to 0.8513 vs supervised dense 0.9028, but filter-in-loop is still best for supervised dense at 452.64 m terminal RMSE vs dense JEPA+fine-tune 459.84 m; sedenion JEPA+fine-tune remains weak at 1.2525 proxy MSE and 520.78 m terminal RMSE.
- Enriched `uav-viewer` with procedural Three.js scenery for the simulator: runway deck/markings/lights, apron, hangars, field bands, beacon masts, treeline, sky gradient, sun/halo, cloud bank, star field, operations tower, radar dish/sweep, radome, perimeter fence, searchlight beams, and subtle animated sensor motion. Visual direction: quiet instrumented UAV test range, closer to Syd Mead's industrial runway staging filtered through restrained NASA/mission-control utility than a decorative game map.
- Improved `uav-viewer` from visual simulator toward briefing UX: renamed the surface to `UAV Range`, added a top HUD rail (phase/speed/alt/nav error/link), mission-status panel for GPS-denied defense context, layer toggles for trajectory/vectors/airspace/range, and camera presets for Chase/Orbit/Top/Command. Verified build plus desktop/mobile render and control interactions on the live `127.0.0.1:5174` Vite server.
- Localized the entire visible `uav-viewer` UI to pt-BR: `Campo VANT`, HUD labels, mission text, layer controls, camera presets, playback controls, readout labels, loading/error strings, and `lang="pt-BR"`.
- Further polished `uav-viewer` presentation UX: default playback is now `0.75x`, default camera is `Órbita`, the playback multiplier is visible beside `Velocidade`, `Apresentação` mode hides operational panels (`stats`, layers, readout, drop zone), and trajectory comparison now draws an optional truth trail from `gx/gy/gz` when present.
- Updated `nav-repr-bakeoff` JEPA pretraining after reviewing late-2025/2026 video and trajectory JEPA recipes: added a V-JEPA-style EMA target encoder and latent predictor while keeping the physical accel-bias/future-drift target. On `data/arrow/proc16-realbias-realtrain-realtest`, Sedenion JEPA+finetune improved from the prior 341.70 m to 322.77 m terminal RMSE, still behind supervised Sedenion at 309.54 m. Predictor warmup and stronger/weaker latent weights regressed, so the kept recipe is EMA future-latent weight 0.5 with default bias sigma calibration.
- Switched `uav-viewer` scenery fully to daylight: brighter blue sky/fog/sun/ground/grid palette, denser daytime cloud/contrail detail, clearer warm horizon glow and distant ridge layers, and no night star field while preserving pt-BR HUD contrast. Follow-up cleanup softened the horizon planes with alpha gradients, reduced daylight searchlight artifacts, and smoothed/toned down the noisy trajectory hairball near the pad.
- Added lightweight desktop, presentation, and mobile simulator screenshots under `docs/assets/` and linked them from the root README's `uav-viewer` section.
