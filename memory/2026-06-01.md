2026-06-01

- Updated repository README and crate READMEs to concise, factual reproduction instructions.
- Added or adjusted experiment commands and evidence file locations.
- Verified toolchain pin in rust-toolchain.toml.

Commands used:
- cd sedenion && cargo test --release
- cd repr-bakeoff && python3 tools/ref_pure.py && cargo run --release
- cd nav-bakeoff && cargo run --release --bin nav-bakeoff
