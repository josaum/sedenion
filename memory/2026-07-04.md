# 2026-07-04

- Set up the Cloud dev environment for the `sedenion` repo (3 Rust crates + `uav-viewer` + Python tools), verified lint/test/build/run for all, and ran the viewer end-to-end after generating `nav-default.arrow` via `export-uav-arrow`. Added a `## Cursor Cloud specific instructions` section to `AGENTS.md` (merged as PR #8).
- Started roadmap item #1 (the real-data representation path from `drone-datasets.md`). Stood up a `nav-bakeoff/.rosenv` Python venv (pyarrow 24, rosbags 0.11.3, numpy 2.5, datasets 5.0, huggingface_hub 1.22); needed `apt install python3.12-venv` first for `ensurepip`. `rosbag_to_arrow.py --self-test` round-trips the Arrow schema OK.
- Fetched real Edged-USLAM DAVIS346 motion bags from HF (`sebnem-byte/drone-navigation-event-camera`, unauthenticated) and converted 5 to Arrow with `hf_edged_uslam_to_arrow.py` (world-calib gravity mode): `dark_manual_filtered`, `circle_filtered`, `line`, `auto_circle_unfiltered`, `square`. `line`/`square` (topic `/mavros/vision_pose/pose`) have clean near-zero `a_nav`; the vicon-pose circle bags leave more gravity residual.
- Ran `nav-repr-bakeoff --data` end-to-end on real flights for the first time. Results are real but small-sample (filter-in-loop n = #held-out files):
  - 5 real flights, `--test-frac 0.4` (3 train / 2 test): learned aiding beats dead-reckoning significantly for Dense JEPA+finetune (−151 m, t=−2.26*) and Sedenion JEPA+finetune (−109 m, t=−7.73*). Dead reckoning term RMSE 381.82 m.
  - Mixed 16 procedural + 2 real train / 2 real held-out: the compact **Sedenion 16D** is the best significant learned model (−59.6 m, t=−2.62*), while dense JEPA variants regress. First signal that the sedenion representation prior may generalize better than dense JEPA under procedural-heavy pretraining + real held-out test — worth confirming with more real bags.
- No Rust/tool code changes were needed; the real-data path worked as written. Committed only `.gitignore` (ignore `.rosenv/` and `/data/`) and docs. Next: pull more real bags for ≥4 held-out sequences to get stronger t-stats, and sweep the JEPA/sedenion recipe on real data.
