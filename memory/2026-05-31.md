# 2026-05-31

- First workspace session loaded `SOUL.md` and `USER.md`; no prior daily memory or `MEMORY.md` existed.
- Removed `BOOTSTRAP.md` after reading it, per workspace bootstrap instructions.
- Fixed VS Code Todo Tree ripgrep lookup by adding `.vscode/settings.json` with `todo-tree.ripgrep` set to `/opt/homebrew/bin/rg`.
- Repaired the SIGReg/LeJEPA PR cleanup: verified the pure-Python reference value (`2.07709580`), reran `repr-bakeoff` synthetic and MNIST outputs, restored `min_std` to expose scale collapse, and replaced stale README claims/tables with measured fixed-code results.
- Superseded intermediate result: after an initial margin-style ZDA-Reg change, `repr-bakeoff` checks passed and synthetic improved, but MNIST stayed at 10.3% for low tested `λ_zda`; this was later superseded by the scale-invariant margin sweep below.
- Superseded raw-λ result: user correctly flagged identical MNIST accuracy across low `λ_zda` as a smell. A wider raw-λ sweep moved MNIST, but this was later replaced by the parameter-free auto-ZDA implementation below.
- Superseded the raw-λ sweep with parameter-free auto-ZDA: ZDA is now a `-log(score)` zero-divisor barrier plus N(0,I) norm floor, gradient-balanced against SIGReg/invariance and RMS-boosted under collapse. Current official results: synthetic auto-ZDA 92.8% vs dense 90.3%; MNIST auto-ZDA 41.0% vs dense 30.7%; tests pass.
- Revisited `nav-bakeoff`: tests pass; linear MEMS result still supports the negative conclusion (aided baseline / SUKF λ=0 at 20.8 m, λ>=0.25 at 35.0 m; dead reckoning identical at 1346.8 m). Duffing 16-seed run reproduces 19.4 m vs 23.1 m; 32-seed run is 21.5 m vs 25.4 m with same conclusion.
- Cleaned nav docs/commands: because the crate has both `nav-bakeoff` and `bilinear-probe` binaries and is not a Cargo workspace, use `cargo run --release --bin nav-bakeoff` and `cargo run --release --bin bilinear-probe`, not the stale `-p nav-bakeoff` commands.
- Deleted legacy generated artifacts: untracked `inspect_grads*` scratch sources/binaries and Cargo `target` directories. `sedenion/target` was tracked legacy build output, so it now appears as intended deletions and should stay out of version control.
- Extracted auto-ZDA from `repr-bakeoff` into the `sedenion` crate as the reusable deliverable API (`zda_score`, `zda_loss_and_grad`, `zda_batch_loss_and_grad`, `auto_zda_gradient_scale`). `repr-bakeoff` now calls the crate API. Synthetic and MNIST outputs stayed the same after extraction.
- Pinned the repo to Rust `1.96.0` in `rust-toolchain.toml` after confirming literal `1.9.6` does not exist and the pasted release note is `Version 1.96.0 (2026-05-28)`. Verified `sedenion`, `repr-bakeoff`, and `nav-bakeoff` tests under the pin.
