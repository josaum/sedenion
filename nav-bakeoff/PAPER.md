nav-bakeoff paper artifacts

This document contains the experiment description, commands to reproduce results, and mappings between claims and code artifacts.

Reproducibility commands
- Run unit tests and the bakeoff binaries in nav-bakeoff to reproduce tables and CSVs.
- See OPERATOR_ALGEBRA.md for operator tests and bilinear probe usage.

Data and artifacts
- bakeoff_results.csv and nav_repr_results.csv contain numeric experiment outputs.
